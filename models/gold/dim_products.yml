version: 2

models:
  - name: dim_products
    description: "Product catalog dimension from Silver."
    columns:
      - name: product_sk
        description: "Surrogate key"
        tests:
          - unique
          - not_null
      - name: product_id
        description: "Business key"
        tests:
          - not_null
